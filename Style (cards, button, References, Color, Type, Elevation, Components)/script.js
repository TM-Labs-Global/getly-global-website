const art=document.querySelector('.hero-card');window.addEventListener('pointermove',e=>{if(!art)return;const x=(e.clientX/innerWidth-.5)*8,y=(e.clientY/innerHeight-.5)*5;art.style.transform=`translate(${x}px,${y}px) rotate(9deg)`});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const el=document.querySelector(a.getAttribute('href'));if(el){e.preventDefault();el.scrollIntoView({behavior:'smooth'})}}));